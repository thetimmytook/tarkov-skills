<Window x:Class="TarkovPerformanceBenchmark.SubmissionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Submit benchmark"
        Width="520"
        Height="270"
        ResizeMode="NoResize"
        WindowStyle="None"
        BorderBrush="#0C0C0C"
        BorderThickness="1"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <TextBlock Text="BENCHMARK DATA READY"
                   Foreground="{StaticResource AccentBrush}"
                   FontWeight="SemiBold" />
        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock x:Name="SummaryText" FontSize="15" FontWeight="SemiBold" />
            <TextBlock Margin="0,12,0,0"
                       Foreground="{StaticResource MutedBrush}"
                       TextWrapping="Wrap"
                       LineHeight="21"
                       Text="In the form, click the Benchmark JSON field and press Ctrl+V. Submit the form, then return to this app." />
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="92" Click="Cancel_Click" />
            <Button Content="Open form"
                    Width="120"
                    Margin="10,0,0,0"
                    Style="{StaticResource PrimaryButton}"
                    IsDefault="True"
                    Click="OpenForm_Click" />
        </StackPanel>
    </Grid>
</Window>
