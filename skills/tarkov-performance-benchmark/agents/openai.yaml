interface:
  display_name: "Tarkov Performance Benchmark"
  short_description: "Read-only EFT benchmark data collection"
  default_prompt: "Use $tarkov-performance-benchmark to guide a read-only Escape from Tarkov benchmark through the signed Store application and interpret its normalized JSON result."
policy:
  allow_implicit_invocation: true
